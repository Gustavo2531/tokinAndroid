<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gustavomendez.tokin">

    <application
        android:name=".FireApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity2"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".EditPerfilActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".SolicitarActivity" />
        <activity android:name=".AgregarEventoActivity" />
        <activity android:name=".ProfileBanda" />
        <activity
            android:name=".VerEvaluacionesActivity"
            android:label="@string/title_activity_ver_evaluaciones"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".EvaluarActivity" />
        <activity
            android:name=".ChatActivity"
            android:label="@string/title_activity_chat"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".BandOrRestaurantActivity" />
        <activity android:name=".ProfileRestaurante"></activity>
    </application>

</manifest>